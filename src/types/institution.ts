export type institution = {
    id: string
    name: string
    email: string
    phone: string
    zip: string
    city: string
    state: string
    street_address: string
    number: string
    complement: string
    neighborhood: string
    device_count: number
}